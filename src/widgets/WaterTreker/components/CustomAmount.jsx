const CustomWaterEntry = () => {
  return (
    <input
      type="number"
      inputmode="numeric"
      id="customAmountInput"
      placeholder="Custom amount, ml"
      aria-label="Custom water amount in millilitres"
    />
  );
};

export default CustomWaterEntry;
